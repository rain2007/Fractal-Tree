/**
 * Interface for an observer in an observer design pattern
 */
public interface FractalObserver {

    /**
     * Method that is called when the state of the fractal model changes.
     */
    void update();
}
